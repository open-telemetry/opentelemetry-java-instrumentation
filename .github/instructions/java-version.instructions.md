---
applyTo: ".java-version"
---

# Java build version

Keep the required/default build Java version references in `CONTRIBUTING.md` and
`docs/contributing/running-tests.md` consistent with `.java-version`. When
`.java-version` changes, verify both documents and update their references as needed.
