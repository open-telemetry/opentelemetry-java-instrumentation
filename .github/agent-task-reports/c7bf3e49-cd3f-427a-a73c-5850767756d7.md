# Consistency review: PR #20020

## Source verification

The live pull request matched the captured source before review:

- Head repository: `ym0506/opentelemetry-java-instrumentation`
- Head branch: `fix/jdbc-query-sanitization-19644`
- Head SHA: `349e41cf5e4fb0b1026707a62877b3643002cb54`

The inventory below is based only on the live GitHub pull request diff for the four declared
changed files.

## Findings

### 1. Three-level system-property fallback chain

- **Changed code:** `instrumentation/jdbc/library/src/main/java/io/opentelemetry/instrumentation/jdbc/OpenTelemetryDriver.java:84-91`
- **Closest compliant exemplar:** `instrumentation/jdbc/library/src/main/java/io/opentelemetry/instrumentation/jdbc/OpenTelemetryDriver.java:108-111`
- **Frequency:** unique
- **Disposition:** needed
- **Applicable rules and precedence:** The requested JDBC configuration precedence and
  `config-property-stability.md` govern first; the library configuration patterns in
  `library-patterns.md` and the path-local SQL commenter fallback govern next; generic simplicity
  guidance in `general-rules.md` and raw frequency apply last. No Java style or test rule requires a
  different form.
- **Reasoning:** The new fallback is more deeply nested than the adjacent two-level SQL commenter
  fallback, but each level directly represents a distinct supported precedence tier: JDBC,
  common-database, and deprecated common-database configuration. Extracting or flattening it would
  obscure that local precedence pattern rather than improve consistency.

### 2. ExtendedOpenTelemetry mock forwards SDK providers and supplies declarative configuration

- **Changed code:** `instrumentation/jdbc/library/src/test/java/io/opentelemetry/instrumentation/jdbc/OpenTelemetryDriverQuerySanitizationTest.java:115-145`
- **Closest compliant exemplar:** `instrumentation-api/src/test/java/io/opentelemetry/instrumentation/api/instrumenter/SpanSuppressionStrategyTest.java:93-99`
- **Frequency:** unique
- **Disposition:** needed
- **Applicable rules and precedence:** The Java test instructions and
  `testing-general-patterns.md` govern first; the JDBC library integration-test requirements and
  `library-patterns.md` govern next; the compliant `ExtendedOpenTelemetry` mocking exemplar and
  generic test readability guidance apply afterward. `testing-semconv-stability.md` does not
  require a different fixture shape.
- **Reasoning:** The helper is more elaborate than the closest compliant
  `ExtendedOpenTelemetry` mock because this driver test needs both working SDK providers for
  emitted telemetry and instance-specific declarative configuration. The exemplar validates
  mocking the extended API and both configuration access paths; no reusable fixture provides the
  required combined behavior.

### 3. Explicit null stubs for higher-priority deep-stubbed configuration values

- **Changed code:** `instrumentation-api-incubator/src/test/java/io/opentelemetry/instrumentation/api/incubator/config/internal/DbConfigTest.java:170-175`
- **Closest compliant exemplar:** `instrumentation-api-incubator/src/test/java/io/opentelemetry/instrumentation/api/incubator/semconv/messaging/internal/MessagingConfigTest.java:126-135`
- **Frequency:** rare
- **Disposition:** needed
- **Applicable rules and precedence:** The Java test instructions and configuration fallback
  expectations in `testing-general-patterns.md` and `config-property-stability.md` govern first;
  the compliant nearby incubator configuration-test exemplar governs next; generic brevity
  guidance and raw frequency apply last.
- **Reasoning:** Although verbose, the explicit null stubs match the nearby compliant
  configuration-test pattern and document traversal through every higher-priority setting to the
  deprecated fallback. The precedent does not conflict with the Java test rules, and removing the
  stubs would make the intended fallback path less explicit.

### Ordered avoidable items

None. There are no avoidable findings, so no rule-conflict gates or fix commits are required.

## Result

No consistency changes were made. The review found three uncommon patterns, all needed, and no
avoidable or unclear findings.

## Validation

- Rechecked every exemplar against the applicable Java style and Java test instructions.
- Confirmed the workspace remained unchanged from source head before adding this report.
- No code validation was required because there were no code changes.
