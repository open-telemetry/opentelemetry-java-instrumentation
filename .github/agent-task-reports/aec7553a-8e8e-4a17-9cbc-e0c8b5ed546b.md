# Self-review report

The pull request is cleared: one complete review iteration found no remaining code, security,
maintainability, or test-coverage findings.

Validation:

- `./gradlew :instrumentation:jedis:jedis-1.4:javaagent:unitTests :instrumentation:jedis:jedis-1.4:javaagent:test :instrumentation:jedis:jedis-1.4:javaagent:testStableSemconv`
  — passed (262 actionable tasks).
- Existing PR checks, including the required build, CodeQL, latest-dependency tests, muzzle,
  Spotless, and lint, passed for the reviewed head.

The title remains accurate. The description should describe the shared bounded target
representation instead of claiming that every configured shard is always emitted.

```json
{
  "findings": [],
  "repository": {
    "owner": "open-telemetry",
    "name": "opentelemetry-java-instrumentation",
    "head": {
      "ref": "trask-redis-jedis-1-4-targets",
      "sha": "36e7776daa9279bbb748536b88fc0490477f0358"
    },
    "base": {
      "ref": "main",
      "sha": "e1529f208b8c122de9aeef74304290ca05c3c08f"
    }
  },
  "pull_request": {
    "number": 20074,
    "url": "https://github.com/open-telemetry/opentelemetry-java-instrumentation/pull/20074"
  },
  "iterations_used": 1,
  "metadata": {
    "current": {
      "title": "Report configured Redis targets for Jedis 1.4",
      "body": "Reports configured Redis targets in Jedis 1.4 spans and metrics when stable database semantic conventions are enabled.\n\nDirect and pooled clients report their configured host and port. Sharded clients report every configured shard in its configured order instead of only the selected shard. Legacy semantic convention output remains unchanged."
    },
    "proposed": {
      "title": "Report configured Redis targets for Jedis 1.4",
      "body": "Reports configured Redis targets in Jedis 1.4 spans and metrics when stable database semantic conventions are enabled.\n\nDirect and pooled clients report their configured host and port. Sharded clients report a bounded configured shard list in its configured order (up to five complete endpoints and 255 characters) instead of only the selected shard. Legacy semantic convention output remains unchanged."
    }
  }
}
```
