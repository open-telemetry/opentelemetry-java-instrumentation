Move review rules into GitHub Copilot's repository-wide and path-specific instructions so its reviewer can apply the checks and exceptions without loading linked knowledge articles.

Treat the articles as on-demand implementation references: each says when it helps and retains technical examples and procedures without duplicate review checklists. The module-cleanup agent uses the native rules and loads detail when a code change needs it, while still checking configuration reads against metadata.
