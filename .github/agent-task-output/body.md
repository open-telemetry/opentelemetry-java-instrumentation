Extends the package-name checker to validate both library and javaagent source sets and documents the remaining package and module naming exceptions.

The checker now derives expected packages from versioned module names and fails when source discovery commands fail.
