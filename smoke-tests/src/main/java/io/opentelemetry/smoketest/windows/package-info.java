/**
 * This package implements the container management for Windows containers, which Testcontainers
 * does not currently support (it only supports running Linux containers on Windows, but not Windows
 * containers). As soon as Testcontainers add full support for Windows containers, the need for this
 * package should be reevaluated.
 */
package io.opentelemetry.smoketest.windows;
