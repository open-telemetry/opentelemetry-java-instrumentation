apply from: "${rootDir}/gradle/java.gradle"

minimumBranchCoverage = 0.5
minimumInstructionCoverage = 0.6

dependencies {
  implementation deps.opentelemetryApi
  implementation deps.guava

  annotationProcessor deps.autoservice
  implementation deps.autoservice

  testImplementation deps.opentelemetrySdk
  testImplementation project(':testing')
  testImplementation project(':utils:gc-utils')
}

test {
    systemProperty("java.util.logging.config.file", "${projectDir}/src/test/resources/test-logging.properties")

}
