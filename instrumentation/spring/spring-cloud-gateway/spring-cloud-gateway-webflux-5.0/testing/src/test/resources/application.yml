spring:
  cloud:
    gateway:
      server:
        webflux:
          routes:
            - uri: h1c://mock.response
              predicates:
                - Path=/gateway/echo
              order: 2023