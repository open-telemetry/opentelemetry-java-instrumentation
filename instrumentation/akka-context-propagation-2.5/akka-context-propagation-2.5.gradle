apply from: "$rootDir/gradle/instrumentation.gradle"
apply from: "$rootDir/gradle/test-with-scala.gradle"

muzzle {
  pass {
    // because this pulls in java-concurrent, it pulls in all its dependencies as well
    extraDependency('org.jetbrains.kotlin:kotlin-stdlib-common:1.3.72')
    extraDependency('org.jetbrains.kotlinx:kotlinx-coroutines-core:1.3.7')
    group = 'com.typesafe.akka'
    module = 'akka-actor_2.11'
    versions = "[2.5.0,)"
  }
}

dependencies {
  implementation project(':instrumentation:java-concurrent')
  compileOnly group: 'com.typesafe.akka', name: 'akka-actor_2.11', version: '2.5.0'

  testImplementation deps.scala
  testImplementation group: 'com.typesafe.akka', name: 'akka-actor_2.11', version: '2.5.0'
}