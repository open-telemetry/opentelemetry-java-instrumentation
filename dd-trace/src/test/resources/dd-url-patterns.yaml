decorators:
  - type: HTTPComponent
    matchingValue: hello
    setValue: world
  - type: HTTPComponent
    matchingValue: foo
    setValue: bar
  - type: URLAsResourceName

# Configuration of the URLAsResourceName
urlResourcePatterns:
  - regex: ".*"
    replacement: "foo"
  - regex: "foo"
    replacement: "bar"
